# 🚀 План развития кастомного чата "Беседка" на Django Channels

**Версия:** 3.0 - КРИТИЧЕСКИЙ АНАЛИЗ СИСТЕМЫ НЕПРОЧИТАННЫХ СООБЩЕНИЙ
**Дата создания:** 26 июня 2025
**Последнее обновление:** 5 июля 2025
**Статус:** **КРИТИЧЕСКИЕ ПРОБЛЕМЫ UX ВЫЯВЛЕНЫ - ТРЕБУЕТСЯ ПЕРЕОСМЫСЛЕНИЕ ЛОГИКИ**

---

## 📚 1. Полезные ресурсы и источники вдохновения

- **[Официальный туториал Django Channels](https://channels.readthedocs.io/en/latest/tutorial/part_2.html)** - Наша основа для построения backend-части.
- **[Пример Django-ChatApp на GitHub](https://github.com/rustyxlol/Django-ChatApp)** - Источник идей для реализации конкретных механик, но не для слепого копирования.
- **[Telegram Web](https://web.telegram.org/)** - Эталонная реализация системы непрочитанных сообщений для копирования логики
- **[WhatsApp Web](https://web.whatsapp.com/)** - Дополнительный референс современных мессенджеров

---

## 🎯 2. Глобальная цель

Создать полностью кастомизируемый, производительный и красивый чат, глубоко интегрированный в экосистему "Беседки". Чат должен соответствовать высоким стандартам UI/UX, определенным в **[BESEDKA_UI_STANDARDS.md](BESEDKA_UI_STANDARDS.md)**, и нативно поддерживать систему ролей из **[BESEDKA_USER_SYSTEM.md](BESEDKA_USER_SYSTEM.md)**.

**🚨 КРИТИЧЕСКОЕ ОБНОВЛЕНИЕ:** В результате детального анализа и тестирования выявлены фундаментальные проблемы в логике системы непрочитанных сообщений, требующие полного переосмысления подхода.

Мы отказываемся от `iframe` и "черных ящиков" в пользу полного контроля над кодом, версткой и логикой.

---

## 🚨 **КРИТИЧЕСКИЙ АНАЛИЗ СИСТЕМЫ НЕПРОЧИТАННЫХ СООБЩЕНИЙ**

### 📱 **АНАЛИЗ ЭТАЛОННОЙ ЛОГИКИ TELEGRAM И WHATSAPP**

#### 🎯 **Определение "непрочитанного" (Telegram-стандарт):**
- **Момент назначения:** Сообщение становится "непрочитанным" в момент его **получения**, если пользователь НЕ находится в чате в этот момент
- **Критерий:** Сообщение создано **ПОСЛЕ** последней активности пользователя в данном чате
- **Исключение:** Если пользователь активен в чате (окно открыто, не свернуто), новые сообщения сразу помечаются как прочитанные

#### ⏱️ **Снятие флага "непрочитанное" (Telegram-стандарт):**
- **Условие 1:** Сообщение **полностью видно** на экране
- **Условие 2:** Пользователь **активен** (окно не свернуто, есть фокус)
- **Задержка:** ~3-5 секунд непрерывной видимости
- **Критерий остановки:** Пользователь НЕ взаимодействует с UI (не скроллит, не кликает)

#### 🎯 **Позиция при возвращении (Telegram-стандарт):**
- **Правило:** Всегда к **первому непрочитанному** сообщению
- **Исключение:** Если непрочитанных нет - к последнему сообщению
- **Скролл:** Первое непрочитанное позиционируется в **верхней трети** экрана (НЕ по центру!)

#### 🔵 **Логика плавающей кнопки (ФИНАЛЬНЫЙ Telegram-стандарт):**
- **"К непрочитанным"** (синий конверт + число непрочитанных) - переход к **первому** непрочитанному
- **"Вниз"** (серая стрелка без числа) - к последнему сообщению когда нет непрочитанных, но не в конце чата
- **Скрытая кнопка** - когда находимся в конце чата (нет сообщений ниже)
- **Позиционирование:** Первое непрочитанное в верхней трети экрана

### ❌ **ВЫЯВЛЕННЫЕ КРИТИЧЕСКИЕ ПРОБЛЕМЫ В ТЕКУЩЕЙ РЕАЛИЗАЦИИ**

#### **1. Отсутствие счетчика непрочитанных в конверте:**
```javascript
// ПРОБЛЕМА: Конверт показывается без числа непрочитанных
<i class="fas fa-envelope"></i>
// TELEGRAM-СТАНДАРТ: Конверт ВСЕГДА с числом
<i class="fas fa-envelope"></i><span class="unread-badge">8</span>
```

#### **2. Неправильная логика определения позиции при загрузке:**
```python
# ПРОБЛЕМА: Система всегда переносит в одно место без объяснения логики
# Пользователь всегда попадает в середину чата при обновлении страницы
```
**Telegram-стандарт:** При входе в чат пользователь должен попасть либо к первому непрочитанному, либо в конец чата.

#### **3. Стрелка вниз показывается там, где должен быть конверт:**
```javascript
// ПРОБЛЕМА: При наличии непрочитанных показывается стрелка вместо конверта
// На скриншотах видна стрелка в середине чата, где логически есть сообщения ниже
```
**Telegram-стандарт:** Если есть непрочитанные - ТОЛЬКО конверт с числом, стрелка ТОЛЬКО когда непрочитанных нет.

#### **4. Отсутствие видимых счетчиков непрочитанных:**
```javascript
// ПРОБЛЕМА: Backend знает о 8 и 5 непрочитанных, но UI их не показывает
// Система определяет позицию, но не отображает количественные данные
```
**Telegram-стандарт:** Пользователь должен видеть точное количество непрочитанных в конверте.

#### **5. Неясная граница между прочитанными и непрочитанными:**
- **Текущая проблема:** Система позиционирует пользователя в произвольном месте без объяснения критериев
- **Telegram-решение:** Четкий разделитель "Непрочитанные сообщения" И конверт с числом

#### **6. Несоответствие состояния UI и данных:**
- **Проблема:** Backend имеет данные о непрочитанных, но frontend отображает стрелку вместо конверта
- **Необходимо:** Синхронизация backend-данных с правильным frontend-отображением

---

## 🎯 **ДЕТАЛЬНЫЙ ПЛАН РЕШЕНИЯ ПО ЭТАПАМ**

### **🚀 ЭТАП 1: Правильная логика кнопки навигации (КРИТИЧЕСКИЙ ПРИОРИТЕТ)**

#### **1.1. Реализация ТОЧНОГО Telegram-стандарта:**
- **HTML структура:** `<button><i class="icon"></i><span class="counter"></span></button>`
- **Логика отображения:** Конверт ТОЛЬКО при непрочитанных, стрелка ТОЛЬКО при отсутствии непрочитанных
- **Счетчик:** ТОЛЬКО в конверте, стрелка ВСЕГДА без числа

#### **1.2. JavaScript логика кнопки:**
```javascript
function updateNavigationButton(unreadCount, isAtBottom) {
    const button = document.getElementById('floating-unread-btn');
    const icon = button.querySelector('.nav-icon');
    const counter = button.querySelector('.nav-counter');
    
    if (unreadCount > 0) {
        // КОНВЕРТ С ЧИСЛОМ - есть непрочитанные
        icon.className = 'fas fa-envelope nav-icon';
        counter.textContent = unreadCount;
        counter.style.display = 'inline';
        button.style.display = 'block';
    } else if (!isAtBottom) {
        // СТРЕЛКА БЕЗ ЧИСЛА - нет непрочитанных, но не в конце
        icon.className = 'fas fa-arrow-down nav-icon'; 
        counter.style.display = 'none';
        button.style.display = 'block';
    } else {
        // СКРЫТЬ - в конце чата
        button.style.display = 'none';
    }
}
```

#### **1.3. CSS стилизация счетчика:**
```css
.nav-counter {
    background: #ff4757;
    color: white;
    border-radius: 10px;
    padding: 2px 6px;
    font-size: 12px;
    margin-left: 5px;
    min-width: 16px;
    text-align: center;
}
```

### **🎯 ЭТАП 2: Правильная логика позиционирования при загрузке (КРИТИЧЕСКИЙ ПРИОРИТЕТ)**

#### **2.1. Определение стартовой позиции:**
1. **Есть непрочитанные:** Позиция к первому непрочитанному (верхняя треть экрана)
2. **Нет непрочитанных:** Позиция к последнему сообщению
3. **Первый визит:** Позиция к последнему сообщению, все текущие = прочитанные

#### **2.2. Исправление модели данных:**
```python
class UserChatPosition:
    last_read_at = models.DateTimeField()
    first_visit_at = models.DateTimeField(null=True, blank=True)  # NEW
    scroll_position_message_id = models.IntegerField(null=True)  # NEW
    
    def get_unread_count_fixed(self):
        """Правильная логика для новых пользователей"""
        if self.first_visit_at is None:
            # Новый пользователь - устанавливаем первый визит
            self.first_visit_at = timezone.now()
            self.last_read_at = timezone.now()
            self.save()
            return 0
        
        # Непрочитанные только ПОСЛЕ последнего прочтения
        return self.room.messages.filter(
            created_at__gt=self.last_read_at
        ).count()
```

#### **2.3. Алгоритм определения стартовой позиции:**
```javascript
function determineStartPosition(unreadCount, lastReadMessageId) {
    if (unreadCount > 0 && lastReadMessageId) {
        // К первому непрочитанному (в верхней трети)
        const firstUnread = findFirstUnreadMessage(lastReadMessageId);
        return scrollToUpperThird(firstUnread);
    } else {
        // К последнему сообщению
        return scrollToBottom();
    }
}
```

### **⚡ ЭТАП 3: Отображение количественных данных в UI (ВЫСШИЙ ПРИОРИТЕТ)**

#### **3.1. Диагностический интерфейс:**
- **Debug панель:** Временное отображение значений `unreadCount`, `messagesBelow`, `currentPosition`
- **Консольное логирование:** Подробная информация о расчетах позиции и счетчиков
- **Визуальные индикаторы:** Подсветка границ между прочитанными и непрочитанными

#### **3.2. Правильный разделитель:**
```javascript
function updateUnreadSeparator(firstUnreadMessageId, unreadCount) {
    const existingSeparator = document.querySelector('.unread-separator');
    if (existingSeparator) existingSeparator.remove();
    
    if (unreadCount > 0 && firstUnreadMessageId) {
        const firstUnread = document.querySelector(`[data-message-id="${firstUnreadMessageId}"]`);
        if (firstUnread && isMessageVisible(firstUnread)) {
            insertSeparatorBefore(firstUnread, unreadCount);
        }
    }
}
```

#### **3.3. Счетчики в реальном времени:**
- **WebSocket события:** `unread_count_updated`, `position_changed`
- **Автообновление:** Пересчет при новых сообщениях, изменении позиции
- **Throttling:** Ограничение частоты обновлений для производительности

### **📱 ЭТАП 4: Точное позиционирование (ВЫСШИЙ ПРИОРИТЕТ)**

#### **4.1. Позиционирование в верхней трети:**
```javascript
function scrollToUpperThird(targetElement) {
    const container = document.getElementById('chat-messages-area');
    const containerHeight = container.clientHeight;
    const targetPosition = targetElement.offsetTop - (containerHeight * 0.25);
    
    container.scrollTo({
        top: targetPosition,
        behavior: 'smooth'
    });
    
    // Подсветка целевого сообщения
    highlightTargetMessage(targetElement);
}
```

#### **4.2. Подсчет сообщений ниже текущей позиции:**
```javascript
function countMessagesBelow(currentScrollPosition) {
    const container = document.getElementById('chat-messages-area');
    const visibleBottom = currentScrollPosition + container.clientHeight;
    const messages = document.querySelectorAll('.message');
    
    let count = 0;
    messages.forEach(message => {
        if (message.offsetTop > visibleBottom) {
            count++;
        }
    });
    
    return count;
}
```

### **🔄 ЭТАП 5: Умная автоотметка и производительность (СРЕДНИЙ ПРИОРИТЕТ)**

#### **5.1. Telegram-like автоотметка:**
- **Задержка:** 4-5 секунд полной видимости
- **Требование:** 100% видимости сообщения
- **Детектор скорости:** Блокировка при быстрой прокрутке (>1000px/sec)
- **Пауза:** 1 секунда после остановки скролла

#### **5.2. Обработка больших объемов:**
- **Счетчик "999+"** для больших количеств
- **Виртуализация** для тысяч сообщений
- **Постепенная отметка** без массовых операций

---

## 🔧 **ТЕХНИЧЕСКАЯ АРХИТЕКТУРА РЕШЕНИЯ**

### **1. Backend (Django):**
```python
class SmartChatPosition:
    def mark_as_read_gradually(self, visible_messages, view_time):
        """Telegram-like постепенная отметка"""
        
    def get_first_unread_position(self):
        """Точная позиция первого непрочитанного"""
        
    def calculate_smart_scroll_target(self, viewport_height):
        """Позиция в верхней трети экрана"""
        
    def detect_first_visit(self):
        """Определение первого визита для правильной логики"""
```

### **2. Frontend (JavaScript):**
```javascript
class SmartScrollTracker {
    detectViewportMessages()     // Полностью видимые сообщения
    trackReadingBehavior()       // Скорость скролла, остановки
    positionFirstUnread()        // Правильное позиционирование
    handleMassUnread()           // Сценарий "999+ сообщений"
    detectScrollSpeed()          // Детектор быстрой прокрутки
}
```

### **3. UX компоненты:**
- **Умный счетчик:** "8", "99", "999+" с правильной логикой
- **Разделитель:** Только если есть непрочитанные В ЗАГРУЖЕННОЙ истории
- **Прогресс бар:** При массовом прочтении (опционально)

---

## 🎯 **КЛЮЧЕВЫЕ ПРИНЦИПЫ НОВОГО ПОДХОДА**

### **1. "Telegram-First" философия:**
- Копируем поведение Telegram 1:1
- Пользователи уже знают эту логику
- Проверенное UX решение

### **2. Постепенность вместо массовости:**
- НЕ отмечать все разом
- Постепенная отметка по мере просмотра
- Уважение к намерению пользователя

### **3. Предсказуемость:**
- Четкие правила когда что происходит
- Нет "магического" поведения
- Видимая обратная связь

### **4. Производительность:**
- Эффективные алгоритмы для больших объемов
- Дебаунсинг и троттлинг
- Умное кеширование позиций

---

## 🗓️ 3. Этапы разработки (Извлечено из BESEDKA_MASTER_DOCUMENTATION.md и CHAT_DEVELOPMENT_ROADMAP.md)

### ✅ **Исторический контекст (до стратегического разворота):** 
- Был создан полноэкранный immersive интерфейс на Django Channels.
- После этого была произведена миграция на Rocket.Chat, где был достигнут высокий уровень кастомизации CSS.
- Были разработаны, но не до конца интегрированы, системы ответов, реакций и прочее.
- **Вывод:** У нас есть огромное количество готового кода и четкое видение продукта.

---

### ✅ **ПРИОРИТЕТ #1: Базовый функционал и система ответов** - ВЫПОЛНЕНО

#### 1. ✅ **Восстановление базового чата на Django Channels** - ВЫПОЛНЕНО
- **Задача:** Воссоздать работающий обмен сообщениями в реальном времени.
- **Backend:** `ChatConsumer` с `group_add`, `group_send`, `group_discard`. ✅
- **Frontend:** `chat_client.js` с WebSocket подключением, отправкой и рендером сообщений. ✅

#### 2. ✅ **💬 СИСТЕМА ОТВЕТОВ И ЦИТИРОВАНИЯ (КЛЮЧЕВАЯ ФУНКЦИЯ)** - ВЫПОЛНЕНО
- **Desktop:** Контекстное меню по правому клику на сообщении → *Ответить* / *Цитировать*. ✅
- **Mobile:** Свайп влево → быстрый ответ. ✅
- **UI/UX:**
    - **Индикатор режима ответа:** Над полем ввода появляется плашка "В ответ на: [текст сообщения]" с кнопкой "отмена" [x]. ✅
    - **Визуальная подсветка:** Исходное сообщение, на которое отвечают, подсвечивается (например, рамкой). ✅
    - **Отображение в чате:** Ответ содержит блок с цитатой (имя автора, текст), при клике на который происходит скролл к исходному сообщению. ✅

#### 3. ✅ **🎭 СИСТЕМА РЕАКЦИЙ** - БАЗОВАЯ ВЕРСИЯ ВЫПОЛНЕНА
- **Быстрые реакции:** При наведении на сообщение появляются 2 иконки (👍, 👎). ✅
- **Полное меню:** Кнопка "..." открывает полное меню эмодзи. ⏳ (В планах)
- **Отображение:** Под сообщением видны иконки реакций и их счетчики. ✅
- **Логика:** Один пользователь - одна реакция на сообщение. Повторный клик снимает реакцию. ✅

#### 4.5. **⏰ УМНЫЕ ВРЕМЕННЫЕ МЕТКИ** - НОВАЯ ЗАДАЧА ⏳
- **Проблема:** Текущие сообщения показывают только время (ЧЧ:ММ), но для старых сообщений непонятно когда именно они были отправлены
- **Решение из Gemini Redesign Prompt:**
  - **Свежие сообщения (< 24 часов):** Обычный формат времени (05:23)
  - **Старые сообщения (> 24 часов):** Относительные временные метки:
    - "3 минуты назад"
    - "вчера" 
    - "2 дня назад"
    - "неделю назад"
  - **Детали при наведении:** Полная дата/время отображается при hover для старых сообщений
- **Техническая реализация:**
  - Функция `formatMessageTime(timestamp)` в `chat_client.js`
  - Логика определения временных интервалов
  - Tooltip с полной датой/временем
- **UX преимущества:** Пользователи сразу понимают контекст сообщения и его актуальность

#### 5. **📬 СИСТЕМА НЕПРОЧИТАННЫХ СООБЩЕНИЙ** - В ПЛАНАХ
- **Разделитель:** В чате появляется линия "Непрочитанные сообщения". ⏳
- **Счетчики:** Badge с количеством непрочитанных сообщений на иконке чата и в списке каналов. ⏳
- **Навигация:** Кнопка "вниз" для перехода между упоминаниями и к последнему сообщению. ⏳

#### ✅ **ДОПОЛНИТЕЛЬНО ВЫПОЛНЕНО:**
- **🌓 СИСТЕМА ПЕРЕКЛЮЧЕНИЯ ТЕМ:** Светлая/темная тема с сохранением в localStorage ✅
- **🎨 УЛУЧШЕННАЯ НАВИГАЦИЯ:** Логичное расположение кнопки переключения темы ✅
- **📝 МОДАЛЬНЫЕ ОКНА:** Редактирование и удаление сообщений ✅
- **📋 КОНТЕКСТНОЕ МЕНЮ:** 6 функций (ответить, переслать, закрепить, редактировать, копировать, удалить) ✅

---

### ⭐ **ПРИОРИТЕТ #2: Система пересылки сообщений** - В РАЗРАБОТКЕ

#### **📤 ДИЗАЙН ПЕРЕСЛАННЫХ СООБЩЕНИЙ (согласно Gemini Redesign Prompt):**

**Концепция:** Четко указывать, что сообщение было переслано из другого чата или контекста, сохраняя его содержимое, но визуально встраивая его в новое сообщение.

**Визуальные элементы:**
- **Контейнер:** Пересланное содержимое отображается как отдельный блок *внутри* стандартной карточки сообщения (либо «своего», либо «чужого»).
- **Иконка и метка:** Небольшая, отчетливая иконка «переслать» (например, изогнутая стрелка ↶ или ➡) с текстом «Переслано из [Имя исходного чата]» (например, «Переслано из Беседка • 👑 Buddy»).
  - `font-size: 0.85em; color: var(--text-secondary-light/dark); font-style: italic;`
- **Отступ/Граница:** Тонкая вертикальная линия (`border-left: 3px solid var(--accent-blue-light/dark);`) вдоль левого края пересланного блока и небольшой левый отступ (`padding-left: 12px;`).
- **Фон:** Очень тонкий, слегка полупрозрачный оттенок акцентного синего (`var(--forwarded-bg-light/dark)`).
  - Светлая тема: `rgba(0, 122, 255, 0.05)` (очень светлый синий наложение).
  - Темная тема: `rgba(0, 122, 255, 0.15)` (немного более заметное синее наложение).
- **Скругленные углы:** `border-radius: 8px;` (немного меньше радиуса основной карточки сообщения для вложенного вида).
- **Содержимое:** Оригинальное содержимое сообщения (текст, медиа) отображается внутри этого блока. Оно сохраняет свое исходное текстовое форматирование, но *без* оригинального стиля пузырька сообщения.

**CSS переменные для пересланных сообщений:**
```css
:root {
    --chat-forwarded-bg: rgba(0, 122, 255, 0.05); /* Светлая тема */
    --chat-forward-border: #D97706; /* Оранжевая граница */
    --accent-blue-light: #007bff;
}

[data-theme="dark"] {
    --chat-forwarded-bg: rgba(0, 122, 255, 0.15); /* Темная тема */
}
```

**Техническая реализация:**
- Функция `parseForwardedMessage()` в JavaScript для определения пересланных сообщений
- CSS классы `.forwarded-own` и `.forwarded-other` для стилизации
- Специальный заголовок `.forwarded-header` с иконкой и текстом источника
- Поддержка как светлой, так и темной темы

---

### ⭐ **ПРИОРИТЕТ #3: Система модерации**

#### 6. **🛡️ СИСТЕМА МУТ/БАН ДЛЯ МОДЕРАТОРОВ И ВЛАДЕЛЬЦА**
- **Контекстное меню:** Дополнительные опции "Mute" и "Ban" в контекстном меню сообщений (только для модераторов и владельца).
- **Модальное окно мута:**
  - Кнопки быстрого выбора времени: 5 мин, 30 мин, 60 мин, 24 часа
  - Поле для ввода произвольного времени
  - Поле для причины мута (опционально)
- **Модальное окно бана:**
  - **Временный бан:** До рассмотрения администраторами (с возможностью связи с модераторами)
  - **Вечный бан:** Красным шрифтом, с обязательным подтверждением
  - Поле для причины бана

#### 7. **🚫 UX ДЛЯ ЗАМУЧЕННЫХ/ЗАБАНЕННЫХ ПОЛЬЗОВАТЕЛЕЙ**
- **Уведомления:** Центральные уведомления о наложенном муте/бане с объяснением ситуации
- **Мут интерфейс:**
  - Красивый обратный счетчик над полем ввода или замена кнопки "Отправить" на крупный красный счетчик
  - Блокировка отправки сообщений с соответствующим сообщением
- **Бан интерфейс:**
  - Полная блокировка доступа к чату
  - Информационное сообщение с возможностью связи с модераторами (для временного бана)
- **Область действия:** Мут/бан касается только раздела чат (глобальные функции будут в админках позже)

### ⭐ **ПРИОРИТЕТ #4: Расширенный функционал**

#### 8. **📎 СИСТЕМА ПРИКРЕПЛЕНИЯ ФАЙЛОВ (MVP)**
- **Задача:** Реализовать загрузку изображений.
- **Интерфейс:** Кнопка-скрепка (📎) возле поля ввода.
- **Процесс:** Выбор файла -> предпросмотр (thumbnail) -> загрузка на сервер -> отправка сообщения с изображением.
- **Типы:** Начать с `JPG`, `PNG`, `GIF`.

#### 9. **📱 МОБИЛЬНАЯ ОПТИМИЗАЦИЯ**
- **Адаптивная верстка:** Корректное отображение на мобильных устройствах.
- **Скрываемая панель:** Список участников по умолчанию скрыт и открывается по кнопке.
- **Жесты:** Базовый свайп для ответа.

---

### 🚨 **ПРИОРИТЕТ #5: КРИТИЧЕСКИЕ ПРОБЛЕМЫ UX (ВЫСШИЙ ПРИОРИТЕТ)**

#### 10. **📬 СИСТЕМА НЕПРОЧИТАННЫХ СООБЩЕНИЙ И ВОССТАНОВЛЕНИЯ ПОЗИЦИИ**
**🎯 КРИТИЧЕСКОЕ ОБНОВЛЕНИЕ:** На основе детального анализа выявлены фундаментальные проблемы в логике.

**❌ ТЕКУЩИЕ ПРОБЛЕМЫ:**
- Неправильное определение "непрочитанного" для новых пользователей
- Агрессивная автоотметка (2 сек вместо 3-5 сек)
- Неточное позиционирование первого непрочитанного
- "Прыжки на 3 сообщения" при навигации
- Отсутствие обработки сценария "1000+ непрочитанных"
- Автоотметка при быстрой прокрутке

**✅ TELEGRAM-СТАНДАРТ К РЕАЛИЗАЦИИ:**
- Первое непрочитанное в верхней трети экрана
- 3-5 секунд полной видимости для отметки
- Детектор скорости прокрутки
- Правильная логика для новых пользователей
- Счетчик "999+" для больших объемов
- Постепенная отметка по мере просмотра

#### 11. **🧭 НАВИГАЦИОННЫЕ КНОПКИ И ПЕРСОНАЛЬНЫЕ УВЕДОМЛЕНИЯ (УТОЧНЁННЫЙ ПЛАН НА ОСНОВЕ АНАЛИЗА)**
**🎯 Цель:** Довести до Telegram-подобного поведения: две кнопки (стрелка вниз для общих, красная "@" для персональных), фикс багов со счётчиками, кликами на цитаты.

**❌ Выявленные баги:**
- Счётчик стрелки застревает после навигации по цитате (не обновляется без скролла).
- Клик на кнопку не сбрасывает счётчик сразу.
- Клик на цитату в пересланном сообщении показывает ошибку "удалено", если оригинал не в загруженной истории.
- "@" кнопка не полностью реализована (не всегда появляется, нет приоритета).

**✅ План исправлений (Telegram-style):**
1. **Стрелка вниз:** Добавить принудительное обновление счётчика после любой навигации/загрузки. После клика — сброс в 0 и скрытие.
2. **Красная "@" кнопка:** Активировать только при персональных (ответы на ваши или @упоминания). Клик — скролл к первому (верхняя треть). Правый клик — "Отметить все как прочитанное". Приоритет над стрелкой.
3. **Цитаты в пересланных:** При клике, если оригинал не в DOM, загружать контекст из БД (10 сообщений до/после) и скроллить. Улучшить ошибку: если удалено — уведомление, но остаться на месте.
4. **Общее:** Тесты с 200+ сообщениями, debounce для счётчиков. Сохранить позицию (не ломать!).

**СТАТУС:** Готово к реализации после бэкапа.

### 🔮 **ДОЛГОСРОЧНЫЕ ПЕРСПЕКТИВЫ (Backlog)**

#### **Функционал чата:**
- **Голосовые сообщения.**
- **Поиск по истории чата.**
- **Личные сообщения (PM).**
- **Предпросмотр ссылок (OpenGraph).**
- **Видеозвонки (WebRTC).**

#### **Расширенная система модерации:**
- **Глобальные банэы на уровне проекта** (интеграция с админками)
- **Расширенные права модераторов** в различных разделах
- **Система жалоб и апелляций**
- **Автоматическая модерация** (фильтры слов, спам-детекция)
- **Журнал модераторских действий** для прозрачности

#### **UI/UX улучшения:**
- **Старославянский шрифт для названия сайта "Беседка"** в панели навигации (требует подбора стабильного шрифта).

---
## 4. Техническая стратегия (на основе анализа `CHAT_DEVELOPMENT_ROADMAP.md`)

#### 1. **Backend (Django Channels)**
- **Модели:** `ChatRoom`, `ChatMessage` (с `parent_message` для ответов), `MessageReaction`.
- **Consumer:** Асинхронный `AsyncWebsocketConsumer` для лучшей производительности.
- **База данных:** Все хранится в PostgreSQL. Redis используется только для `channel_layer`.
- **API:** Создать API эндпоинты (на Django Rest Framework или обычном Django) для загрузки файлов и получения истории сообщений.

#### 2. **Frontend (JavaScript)**
- **Архитектура:** Создать класс `BesedkaChat` для инкапсуляции всей логики:
  ```javascript
  class BesedkaChat {
    constructor(roomId, chatElement) {
      this.roomId = roomId;
      this.chatElement = chatElement;
      this.socket = null;
      // ...
    }
    connect() { /* ... */ }
    sendMessage(text, parentId) { /* ... */ }
    renderMessage(messageData) { /* ... */ }
    initEventListeners() { /* Hover, клики, свайпы */ }
    // ...
  }
  ```
- **Состояние:** Управлять состоянием (режим ответа, id пользователя и т.д.) внутри этого класса.
- **Верстка:** Динамически создавать элементы сообщений из шаблонов `<template>` для производительности.
- **Никаких `iframe`:** Весь рендеринг происходит на стороне клиента в основном документе.

#### 3. **Безопасная разработка и миграция**
1.  **Разработка в изоляции:** Все новые компоненты чата разрабатываются на отдельной странице `/chat/test/` (или аналогичной), не затрагивая работающий сайт.
2.  **Feature Flags:** Использовать флаг в `settings.py` (`CUSTOM_CHAT_ENABLED = True/False`) для переключения между старым (заглушка) и новым чатом.
3.  **Поэтапная замена:** После полного тестирования и вашего одобрения, новый чат заменяет старый на основной странице.
