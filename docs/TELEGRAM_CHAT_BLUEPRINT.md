# 🎯 Чертеж чата "Беседка" (Telegram-like)

**Версия:** 1.0
**Дата создания:** 13 июля 2025
**Статус:** В разработке

## 1. Введение

Этот документ — главный технический и продуктовый чертеж для создания кастомного чата проекта "Беседка", основанный на реверс-инжиниринге и анализе лучших практик официальных клиентов Telegram (Web, Desktop).

**Цель:** Прекратить "изобретение велосипеда" и начать систематическую реализацию проверенных решений. Этот документ является **Единым Источником Правды (SSOT)** для всего, что касается функционала чата.

Он заменяет собой разрозненные планы из `CUSTOM_CHAT_DEVELOPMENT_ROADMAP.md` и объединяет их в единую, структурированную систему.

## 2. Фундамент: Структура сообщения

Все начинается с правильной структуры сообщения. Это основа для рендеринга, ответов, пересылки, реакций и многого другого.

### 2.1. Визуальная декомпозиция (анализ Telegram)

Стандартное сообщение в Telegram состоит из следующих визуальных и логических блоков:

+------------------------------------------------------+
| [AVATAR] (Опционально, в группах)                    |
| +--------------------------------------------------+ |
| | [ИМЯ АВТОРА] (Цветное, в зависимости от роли)    | |
| +--------------------------------------------------+ |
| | [ЦИАТА / ПЕРЕСЫЛКА] (Если есть)                  | |
| |   +--------------------------------------------+ | |
| |   | Имя автора цитаты                          | | |
| |   | Текст цитаты (превью)                      | | |
| |   +--------------------------------------------+ | |
| +--------------------------------------------------+ |
| | [КОНТЕНТ]                                        | |
| |   - Текст сообщения (с форматированием)          | |
| |   - Медиа (картинка, видео, файл)                | |
| +--------------------------------------------------+ |
| | [МЕТАДАННЫЕ]                                     | |
| |   - Время отправки (ЧЧ:ММ)                       | |
| |   - Статус (✓ - отправлено, ✓✓ - прочитано)     | |
| +--------------------------------------------------+ |
| +--------------------------------------------------+ |
| | [РЕАКЦИИ] (Если есть)                            | |
| |   [👍 2] [🎉 1] [🤔 5]                          | |
| +--------------------------------------------------+ |
+------------------------------------------------------+

### 2.2. Наша модель данных (`chat/models.py`) vs. Telegram

**Сравнение:**

| Элемент Telegram | Наша реализация в `Message` модели | Статус и План действий |
| :--- | :--- | :--- |
| **Аватар** | Косвенно через `author.avatar` | ✅ **ГОТОВО**. Используется в UI. |
| **Имя автора** | `author.display_name` | ✅ **ГОТОВО**. Используется в UI, включая значки ролей. |
| **Цитата (Reply)** | `parent = models.ForeignKey(...)` | ✅ **ГОТОВО**. Ключевая функция реализована. |
| **Пересылка (Forward)** | `forwarded_from = models.ForeignKey(...)` | 🟨 **ЧАСТИЧНО**. Модель есть, но UI и логика не до конца реализованы. **ЗАДАЧА: §3.2** |
| **Текст сообщения** | `text_content = models.TextField(...)` | ✅ **ГОТОВО**. Поддержка Markdown. |
| **Медиа** | `media_file = models.FileField(...)` | 🟨 **ЧАСТИЧНО**. Модель есть, UI для разных типов медиа не доработан. **ЗАДАЧА: §6.1, §6.2** |
| **Время отправки** | `timestamp = models.DateTimeField(auto_now_add=True)` | ✅ **ГОТОВО**. Отображается в UI. |
| **Статус** | `read_status = models.CharField(...)` (Custom model `MessageReadStatus`) | 🟨 **ЧАСТИЧНО**. Модель есть, но UI-отображение и логика "прочитано" требуют доработки. **ЗАДАЧА: §5.1, §5.2** |
| **Реакции** | `reactions = models.ManyToManyField(...)` (Custom model `MessageReaction`) | ❌ **НЕ НАЧАТО**. Требует дизайна и реализации. **ЗАДАЧА: §6.4** |
| **Вложения (Inline keyboard)** | ❌ | ❌ **НЕ В ПЛАНЕ**. На данном этапе не приоритет. |
| **Опросы** | ❌ | ❌ **НЕ В ПЛАНЕ**. На данном этапе не приоритет. |

---

## 3. Пересылка сообщений

### 3.1. Визуальное представление (Forwarded Message UI)

Пересланное сообщение должно быть обернуто в специальный блок, содержащий:
- Иконку "Forward" (стрелка).
- Имя оригинального отправителя (цветное, согласно акцентным цветам Telegram).
- Сам контент пересланного сообщения (может быть как текст, так и медиа).
- Отступы и границы, имитирующие вложенность.

### 3.2. Логика пересылки

- Возможность пересылать одно или несколько сообщений одновременно.
- При пересылке сообщений между приватными чатами/группами, отображать имя оригинального отправителя.
- При пересылке из каналов или публичных групп, где отправитель скрыт, отображать название канала/группы.

---

## 4. Редактирование сообщений (Edit Message)

### 4.1. Визуальное представление

- Рядом со временем сообщения появляется пометка "edited" (серого цвета, мелким шрифтом).
- При редактировании сообщения, старый текст полностью заменяется новым.

### 4.2. Логика редактирования

- Возможность редактировать только свои последние сообщения (в течение определенного таймфрейма, например, 24 часа).
- UI-элемент (например, иконка карандаша при наведении или в контекстном меню) для активации режима редактирования.
- Отправка отредактированного сообщения обновляет существующее в БД, а не создает новое.

---

## 5. Удаление сообщений (Delete Message)

### 5.1. Визуальное представление

- Удаленное сообщение исчезает из чата.
- Если сообщение удалено "для всех", оно исчезает у всех участников.
- Если сообщение удалено "для себя", оно исчезает только у пользователя, который его удалил.

### 5.2. Логика удаления

- Опции "Удалить для меня" и "Удалить для всех" (если это собственное сообщение).
- Подтверждение удаления перед фактическим удалением.

---

## 6. Взаимодействия с сообщениями

### 6.1. Изображения и видео (Media)

- Поддержка предпросмотра изображений (thumbnails) и воспроизведения видео непосредственно в чате.
- Возможность открывать медиа в полноэкранной галерее с возможностью листания и зума.

### 6.2. Файлы и документы

- Отображение иконок типов файлов (PDF, DOCX, ZIP и т.д.).
- Отображение имени файла, размера и кнопки скачивания.
- Индикатор прогресса загрузки.

### 6.3. Ссылки (Link Previews)

- Автоматическое генерирование предпросмотров для ссылок, содержащих:
    - Заголовок (крупным шрифтом).
    - Описание (меньшим шрифтом).
    - Изображение (если доступно, справа или сверху).
    - URL (серым цветом).

### 6.4. Реакции (Reactions)

- Отображение эмодзи-реакций под сообщением.
- Индикатор количества реакций для каждого эмодзи.
- Возможность добавить/удалить реакцию, кликнув на нее.
- Всплывающее меню с доступными эмодзи при наведении/долгом нажатии.

---

## 7. UI/UX Стилизация

### 7.1. Цветовая палитра

**Цветовая палитра для чата "Беседка" полностью соответствует Telegram.**
Все точные HEX-коды для светлой и темной тем, а также для различных состояний элементов, находятся в файле **[docs/BESEDKA_UI_STANDARDS.md](docs/BESEDKA_UI_STANDARDS.md)**.

AI-ассистенты должны использовать JSON-данные из [docs/BESEDKA_UI_STANDARDS.md] для определения CSS-переменных.

**Пример структуры CSS-переменных (значения будут динамически заполняться из `BESEDKA_UI_STANDARDS.md`):**

```css
/* Светлая тема */
:root {
  --theme_bg_color: var(--general-ui-background-main-light); /* Основной фон чата */
  --theme_text_color: var(--general-ui-text-primary-light); /* Основной цвет текста */
  --theme_message_out_bg_color: var(--chat-message-out-background-light); /* Фон исходящего сообщения */
  --theme_message_in_bg_color: var(--chat-message-in-background-light); /* Фон входящего сообщения */
  --theme_link_color: var(--general-ui-link-light); /* Цвет ссылок, упоминаний, активных элементов */
  --theme_border_color: var(--general-ui-border-default-light); /* Цвет границ, разделителей */
  --theme_secondary_text_color: var(--general-ui-text-secondary-light); /* Второстепенный текст (время, статусы) */
  --theme_header_bg_color: var(--header-and-menu-header-background-light); /* Фон хедера */
  --theme_input_bg_color: var(--general-ui-background-secondary-light); /* Фон поля ввода */
  /* Добавьте остальные переменные, как определено в BESEDKA_UI_STANDARDS.md */
}

/* Темная тема */
[data-theme="dark"] {
  --theme_bg_color: var(--general-ui-background-main-dark); /* Основной фон чата */
  --theme_text_color: var(--general-ui-text-primary-dark); /* Основной цвет текста */
  --theme_message_out_bg_color: var(--chat-message-out-background-dark); /* Фон исходящего сообщения */
  --theme_message_in_bg_color: var(--chat-message-in-background-dark); /* Фон входящего сообщения */
  --theme_link_color: var(--general-ui-link-dark); /* Цвет ссылок, упоминаний, активных элементов */
  --theme_border_color: var(--general-ui-border-default-dark); /* Цвет границ, разделителей */
  --theme_secondary_text_color: var(--general-ui-text-secondary-dark); /* Второстепенный текст (время, статусы) */
  --theme_header_bg_color: var(--header-and-menu-header-background-dark); /* Фон хедера */
  --theme_input_bg_color: var(--general-ui-background-secondary-dark); /* Фон поля ввода */
  /* Добавьте остальные переменные, как определено в BESEDKA_UI_STANDARDS.md */
}
``` 
