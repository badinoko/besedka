# 📈 Дорожная карта развития чата (Rocket.Chat Embedded)

*Версия 0.1 – начальный сбор всех утверждённых идей и требуемых улучшений*
*Создано: 24 июня 2025 г.*

---

## 1. Текущее состояние

- Интеграция Rocket.Chat полностью функциональна внутри iframe `/chat/integrated/` (HTTP 200).
- Переключение каналов general/vip/moderators работает без кнопки «Join the Channel».
- SSO ↔ Django настроено, резюме-токены формируются автоматически.
- CSS v11.29 и JS v4.2 обеспечивают единый внешний вид (ON-AIR, 3D-кнопки, значки ролей).

## 2. Подтверждённые будущие задачи (сводка из документации)

### 2.1 Ответы на сообщения  
Источник: BESEDKA_UI_STANDARDS.md §«Будущие планы»
- Контекстное меню (desktop) ➜ *Reply* / *Quote*.
- Жесты (mobile): свайп влево → быстрый ответ, свайп вправо → цитирование.
- Визуальная подсветка исходного сообщения.

### 2.2 Мобильная оптимизация  
Источник: BESEDKA_UI_STANDARDS.md / MASTER_DOCUMENTATION.md
- Скрываемый список участников (выдвижная панель).  
- Компактный header: burger-меню + название + счётчик online.
- Оптимизация производительности / lazy load сообщений.

### 2.3 Жестовое управление  
- Свайпы, долгие нажатия, двойные тапы.
- Haptic feedback на поддерживаемых устройствах.

### 2.4 Доработка header чата  
- Объёмный дизайн: статус-иконка (lock/online) слева, название канала по центру, переключатель каналов справа.
- Привязка к утверждённой высоте header (SSOT-стандарт 95 px, padding 0).
- Кликабельный логотип «Беседка» в основной навигации → переход на главную страницу (`/`).
- Использование кириллического шрифта *Cyrillic Old* / *Miroslav* (как в заголовках).

### 2.5 Hover / focus эффекты
- Переход на эффект `scale(1.08)` вместо `translateY` по всему проекту (уже внедрено в chat_switch-buttons, требуется для hero-, action-, form-buttons).

### 2.6 Система прикрепления файлов
- Поддержка изображений, документов, медиа (MP4/MP3).  
- Предпросмотр и прогресс-бар загрузки.

### 2.7 Система реакций и эмодзи
- Реакции ❤️ 😂 😮 👍 к сообщениям.
- Унифицированные счётчики реакций.

### 2.8 Расширенный поиск по чатам
- Фильтрация по тексту, автору, дате, типу вложений.

### 2.9 Видеозвонки / голосовые сообщения (долгосрочная перспектива)
- WebRTC-интеграция для групповых звонков.

---

## 3. Технические принципы реализации

1. **Полная изоляция** – изменения тестируются на `/chat-test/` перед миграцией в боевой `/chat/integrated/`.
2. **SSOT-архитектура** – все повторяющиеся элементы (кнопки, меню, реакции) оформляются как единые компоненты.
3. **Версионирование стилей** – каждая правка CSS ➜ новый `?v=` параметр + перезапуск daphne.
4. **Обратимость** – любые крупные изменения guarded feature-flag `settings.CHAT_NEW_UI=True`.
5. **Безопасность** – проверка ролей/прав согласно BESEDKA_USER_SYSTEM.md.

---

## 4. Открытые вопросы / идеи пользователя

> Сюда добавим новые мысли, которые появятся в ходе обсуждения.  
> (Например: *«Хочу добавить анимацию сияния логотипа при входе в чат»,* *«Нужна кнопка "Закрыть чат" в мобильном режиме»* и т.д.)

### 4.1 Унификация и «оживление» верхней навигации
*Источник: скриншоты 1-2, пользовательский комментарий*
1. Перенести «красивый» стиль навигации (как в режиме чата) на глобальный navbar сайта.  
2. Сделать логотип «Беседка» кликабельным → переход на главную `/`.  
3. Добавить белую подложку/светлую окантовку для слова «Беседка» и иконки.  
4. Glow-эффект (soft-shadow) активного пункта меню – аналог ON-AIR в кнопках.  
5. Обновить единый CSS `navigation.css` (или расширить `unified_styles.css`).

### 4.2 Упрощение интерфейса Rocket.Chat в iframe
*Источник: скриншоты 3-6*
1. Отключить/скрыть лишние пункты контекстных меню (закрепить, экспорт, видеозвонки…).  
2. Оставить базовый набор: ответы, цитирование, реакции (может скрывать по ролям).  
3. Конфигурация через `Layout → Custom CSS` или функцию `RocketChat.hideUI()` в embed-скрипте.  
4. Переопределить доступные разрешения (Permissions API) для ролей owner/moderator/user.

### 4.3 Система непрочитанных сообщений / «как в Telegram»
1. При открытии канала позиционировать на первое непрочитанное сообщение.  
2. Отображать счётчик непрочитанных (badge) на кнопке канала и/или глобально в navbar.  
3. Кнопка «Перейти к последнему» (scrollBottom) + «Отметить всё прочитанным».  
4. Сохранение позиции чтения в Redis/LocalStorage.

### 4.4 Личные сообщения (PM) на сайте
1. Глобальное уведомление/иконка рядом с корзиной (navbar) при новом PM.  
2. Мини-инбокс (dropdown) со списком диалогов, кликом открывает URL `/pm/<username>/`.  
3. Простая страница чата (без реакций/меню) с историей и возможностью «удалить переписку».  
4. Backend: Django model `PrivateMessageThread` + `PrivateMessage`; API на AJAX/HTMX.  
5. Долгосрочно – соединение с Rocket.Chat DM via REST, но начать можно с Django-only.

### 4.5 Кириллический «старый» шрифт
1. Найти надёжные webfont-версии (*ttf/woff2*) шрифта "Cyrillic Old" или аналог.  
2. Встроить через `@font-face` только в заголовки/navbar (ограничить FOUT).  
3. Проверить кэширование и отсутствие 404 – добавить в `static/fonts/` + preconnect hints.

### 4.6 Визуальное разграничение сообщений/авторов
1. Цветовое (или полупрозрачное) выделение имени автора vs. текста сообщения.  
2. Бейджи ролей (Owner, Moderator…) – ярче, с цветной подложкой.  
3. Автоматическое переименование «Admin» → «Moderator».  
4. Разработка дизайн-токенов в `chat_theme.css` (цвет автора, фон сообщения, hover-эффект).

### 4.7 Верхняя плашка чата – счётчики и навигация
1. Разместить в header:  
   • Badge непрочитанных сообщений канала.  
   • Кнопки «↓ к последнему» и «↑ к первому непрочитанному».  
2. Переключатель каналов свести в единое dropdown-меню.  
3. Использовать header и на mobile – адаптивная компоновка.

### 4.8 Mobile-UX
1. Большие, тач-дружелюбные кнопки реакций (tap-hold меню).  
2. Длительное нажатие на сообщение → меню действий (реакция, ответ, цитата).  
3. Floating Action Button «+» для прикрепления медиа.  
4. Swipe-gestures: вправо → answer, влево → меню.

### 4.9 Прикрепление медиа (MVP)
1. Фото (JPG/PNG/WebP) с предпросмотром, макс 5 MB.  
2. Ограничить видео на V-1.0, но предусмотреть API.  
3. Фоновая загрузка + прогресс-бар.  
4. Mobile: доступ к камере / галерее.

### 4.10 Ссылки и предпросмотр
1. Если ссылка содержит OpenGraph → показывать картинку + описание.  
2. YouTube: встраиваемый player с кнопкой play (lazy-load iframe).  
3. Фоллбек – обычная ссылка с иконкой внешнего перехода.

### 4.11 Голосовые сообщения
1. Использовать already-present RC voice-recorder widget в embed.  
2. Ограничить до 2 мин и 2 MB.  
3. Проверить воспроизведение в iframe – при проблемах fallback на download link.  
4. Mobile: удержание кнопки микрофона.

### 4.12 Лимит рабочих мест / пользователей Rocket.Chat
1. Текущий лимит 50 (скрин) – отключить.  
2. Провести ресерч:  
   • `Enterprise → seats` настройки;  
   • env `ROCKETCHAT_LICENSE`, `WORKSPACE_ACCESS_TOKEN`;  
   • Бесплатный self-host не ограничивает users → убедиться.  
3. Тест нагрузкой 1 000 fake users (Locust) – latency & memory.

---

## 5. Следующие шаги

1. Утвердить данный consolid-draft с пользователем.  
2. Создать отдельные GitHub-issues / Trello-карточки под каждый из пунктов 2.x.  
3. Реализовать MVP «Ответы на сообщения» на изолированном `/chat-test/`.  
4. Провести визуальное тестирование + сверить со стандартами UI/UX.  
5. При успешной проверке – мёрж в основную интеграцию `/chat/integrated/`.

---

## 6. Фиксация прогресса

> "Мы двигаемся быстро, но фиксируем шаги ещё быстрее." — принцип честной миграции

Чтобы не засорять основные документы (Master/Standards/User System) и избежать ложных отчётов о «завершённых» задачах, вводится отдельный файл-журнал прогресса `docs/CHAT_DEVELOPMENT_PROGRESS.md`.

### 6.1 Назначение
1. Служит **единственным** местом, где мы фиксируем реальное продвижение по пунктам Roadmap.
2. Каждая запись отражает **только проверенный** результат (визуально или функционально подтверждённый).
3. Ошибки и неудачи фиксируются **на равных** с успехами — журнал не может быть «только зелёным».  
   *Причина: исключаем циклы повторных работ и «эффект забытого бага».*

### 6.2 Формат записи
```
## [YYYY-MM-DD HH:MM] Краткий заголовок результата

**Статус:** ✅ / ❌ / 🔄  (завершено / провал / в процессе)  
**Ответственный:** <ник>  
**Связанный пункт Roadmap:** §2.4 / §4.1 / …  

### Что сделано
- Пунктиром перечисляем конкретные шаги (код, SQL, скрипты, CSS), ссылки на коммиты.

### Результат / Проверка
- Описание фактического вывода в браузере / логи / скриншоты.

### Следующие шаги
- Что нужно доработать, если есть.
```

### 6.3 Правила ведения
1. **Никаких пустых заголовков.** Запись появляется только после факта.
2. **Один пункт — одна запись.** Не смешиваем разные задачи.
3. **Редактируем вместо дублирования.** Если выяснилось, что запись была неточной, корректируем её, а не создаём новую.
4. **Без авто-генерации.** Журнал пишется вручную ИСКЛЮЧИТЕЛЬНО после личной проверки результатов.

### 6.4 Как использовать
1. Выполнили подпункт Roadmap → зафиксировали в журнале.
2. Если работа не доведена до конца — ставим статус 🔄 и чётко описываем, что осталось.
3. После полного подтверждения пользователем — меняем статус на ✅.

*Ссылка-якорь:* все документы могут ссылаться на конкретную запись журнала:  
`[Прогресс §4.1, запись 2025-06-24 18:00](CHAT_DEVELOPMENT_PROGRESS.md#2025-06-24-1800)`

---

*Документ автоматически генерируется из существующей документации и будет поддерживаться как единственный источник правды для развития чата.* 
