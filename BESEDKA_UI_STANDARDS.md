# 🎨 Стандарты UI/UX проекта "Беседка"

**Версия:** 14.0 - Гармонизация SSOT (июнь 2025)
**Дата обновления:** 15 июня 2025 года
**Статус:** **ЭТАЛОН** для всех компонентов интерфейса

---

## 📚 СВЯЗАННЫЕ ДОКУМЕНТЫ

- **[BESEDKA_MASTER_DOCUMENTATION.md](BESEDKA_MASTER_DOCUMENTATION.md)** — Главный документ проекта.
- **[BESEDKA_USER_SYSTEM.md](BESEDKA_USER_SYSTEM.md)** — Эталонная система ролей и пользователей.

---

## 🏛️ 1. ОБЩИЕ ПРИНЦИПЫ И SSOT-АРХИТЕКТУРА

Интерфейс проекта "Беседка" строится на архитектуре **SSOT (Single Source of Truth)**. Все повторяющиеся элементы (карточки, кнопки, счетчики, фильтры, hero-секции, комментарии) должны быть реализованы как универсальные партиалы. **Следование этой архитектуре является правилом по умолчанию.**

- **`core.base_views.UnifiedListView`**: Основной базовый класс для большинства списковых страниц.
- **`templates/base_list_page.html`**: Основной шаблон-скелет для большинства списковых страниц.
- **`templates/includes/partials/unified_card.html`**: Универсальная карточка для любого типа контента.
- **`templates/includes/partials/_unified_cards_wrapper.html`**: Обертка для Masonry-сетки карточек.
- **`templates/includes/partials/unified_detail_hero.html`**: Универсальная hero-секция для детальных страниц.
- **`templates/includes/partials/unified_comment_section.html`**: Единый компонент для секции комментариев.
- **`core.utils.py::get_total_comments_count`**: Единая функция для подсчета всех комментариев (включая вложенные).

---

## 🚦 2. УПРАВЛЯЕМЫЕ ОТКЛОНЕНИЯ ОТ SSOT (КРИТИЧЕСКИ ВАЖНО)

Проект допускает **осознанные, управляемые отклонения** от стандартной SSOT-архитектуры, но только при соблюдении следующих правил:

1.  **Явное требование:** Отклонение реализуется только по вашему прямому запросу, когда стандартные компоненты не подходят для решения уникальной задачи.
2.  **Максимальное переиспользование:** Даже при использовании уникального шаблона, он должен `{% extend %}` от базового (`base.html` или `users/cabinet_base.html`) и включать в себя как можно больше унифицированных партиалов (`_unified_pagination.html`, `unified_comment_section.html` и т.д.).
3.  **Локализация изменений:** Отклонение должно затрагивать только минимально необходимую часть функционала.
4.  **Обязательная документация:** Любое отклонение должно быть зафиксировано в этом документе с описанием причины и сути изменений.

### Пример эталонного отклонения: Страница уведомлений

Страница уведомлений является примером управляемого отклонения:

-   **Отклонение 1: Уникальный шаблон.** Используется `templates/users/notifications_list.html` вместо `base_list_page.html` для реализации дизайна с плитками на всю ширину.
-   **Отклонение 2: Уникальный `paginate_by`.** Используется `paginate_by = 20` вместо стандартных `9` карточек.
-   **Причина:** Требовался уникальный UX, который невозможно было реализовать стандартными карточками.
-   **Соответствие SSOT:** Несмотря на отклонения, страница по-прежнему использует:
    -   Наследуется от `users/cabinet_base.html`.
    -   Использует унифицированную пагинацию `_unified_pagination.html`.
    -   Использует стандартные `hero-filter-btn` и все унифицированные CSS/JS.

### Пример эталонного отклонения: Главная страница (плейсхолдер)

Главная страница проекта «Беседка» (корневой URL `/`) временно реализована как **уникальный шаблон-заглушка** `templates/home/home_placeholder.html`.

- **Отклонение 1: Уникальный шаблон.** Используется отдельный шаблон вместо `base_list_page.html`, так как на главной пока нет карточек/пагинации.
- **Отклонение 2: Уникальный `View`.** Используется `TemplateView` без `UnifiedListView`, так как контент статичен.
- **Причина:** Нужна минимальная стартовая страница, отличная от ленты новостей, для последующего наполнения баннерами и объявлениями.
- **Соответствие SSOT:** Шаблон наследуется от `base.html` и использует унифицированные CSS/JS. При появлении динамического контента главная должна быть приведена к стандарту или обновлена документация.

---

## 🎨 3. ЦВЕТОВЫЕ СХЕМЫ

Цветовая схема определяется автоматически классом родительского контейнера `[section]-hero`.

```css
/* ГАЛЕРЕЯ - ФИОЛЕТОВЫЙ */
.gallery-hero { --section-primary: #6f42c1; --section-secondary: #563d7c; }

/* ГРОУРЕПОРТЫ - ЗЕЛЕНЫЙ */
.growlogs-hero { --section-primary: #28a745; --section-secondary: #20c997; }

/* НОВОСТИ - СИНИЙ */
.news-hero { --section-primary: #007bff; --section-secondary: #0056b3; }

/* МАГАЗИН - ЗЕЛЕНЫЙ */
.store-hero { --section-primary: #28a745; --section-secondary: #1e7e34; }
```

---

## 🔩 4. УНИФИЦИРОВАННЫЕ КОМПОНЕНТЫ

### 4.1. Hero-секция (Плашка)

Компактная, информативная секция с градиентом, анимациями и стеклянными элементами.

- **Структура:** `.hero-stats-and-actions` > `.hero-stats` (счетчики) + `.hero-actions` (кнопки).
- **Стили:** `padding: 1.25rem 0`, `border-radius: 1rem`, фон-градиент, тени.
- **Счетчики:** Адаптивные, стеклянные (`backdrop-filter: blur(10px)`), `max-width: 120px`.
- **Фильтры:** Стеклянные (`backdrop-filter: blur(20px)`), активное состояние с градиентом.

### 4.2. Hero-кнопки (Эталон v23.0)

"Живые" 3D-кнопки с постоянной анимацией переливания.

- **`.hero-btn` (Основная):** Цветной градиент, анимация `shimmer`.
- **`.hero-btn-secondary` (Вторичная):** Стеклянная, полупрозрачная, анимация `glare`.
- **Общие свойства:** 3D-эффект (глубокая тень), всплытие на 6px при наведении.

### 4.3. Карточки контента (`unified_card.html`)

- **Принцип "Вся карточка - ссылка":** Вся область карточки кликабельна и ведет на детальную страницу.
- **Интерактивность:** Лайки, комментарии и другие действия доступны **только** на детальной странице.
- **Структура:** Flex-контейнер с фиксированными `min-height` для дочерних элементов (заголовок, описание) для предотвращения "плавающих" элементов.
- **Счетчики:** Располагаются в `.card-footer`, используют **единый стандарт** (см. п. 5.2).
- **Hover-эффект:** Всплытие карточки (`transform: translateY(-4px)`) и увеличение изображения (`transform: scale(1.05)`).

### 4.3.1. Специфика карточек магазина
Карточки товаров (`card_type == 'store'`) отображают минимальную цену вместо лайков/комментариев.
Стандартный стат `fa-tag` с ценой, выравнивание и стеклянная подложка сохраняются.

### 4.4. Система уведомлений (ПРОТЕСТИРОВАНА И ГОТОВА)

**Глобальная система уведомлений** реализована через единый компонент `window.showNotification()`:

#### 4.4.1. Типы уведомлений:
- **Глобальные:** Системные сообщения для всех пользователей
- **Персональные:** Уведомления о лайках, комментариях для авторизованных пользователей  
- **Системные:** Уведомления от администрации и модераторов

#### 4.4.2. UI/UX стандарты уведомлений:
- **Расположение:** Центр экрана с затемненным фоном
- **Анимация:** Плавное появление и исчезновение (fade-in/fade-out)
- **Автозакрытие:** Через 5 секунд для информационных, 8 секунд для важных
- **Кнопки:** "Перейти" (ведет к связанному объекту), "Закрыть"
- **Стили:** Модальное окно с glassmorphism эффектом, адаптивные размеры

#### 4.4.3. Технические требования:
- **Единый API:** Все уведомления через `core/notifications.py`
- **Правильная навигация:** Кнопки "Перейти" корректно определяют URL связанных объектов
- **AJAX интеграция:** Уведомления появляются без перезагрузки страницы
- **Адаптивность:** Корректное отображение на мобильных устройствах

### 4.5. Унифицированная пагинация (КРИТИЧЕСКИ ВАЖНО)

**Система пагинации работает автоматически через SSOT-архитектуру:**

#### 4.5.1. Архитектурные принципы:
- **`core.base_views.UnifiedListView`** автоматически создает `page_obj` при установке `paginate_by`. По умолчанию `paginate_by = 9`.
- **`templates/base_list_page.html`** автоматически показывает пагинацию при `page_obj.has_other_pages`.
- **`templates/includes/partials/_unified_pagination.html`** — единый шаблон пагинации для всех разделов.

#### 4.5.2. Правила отображения:
- **Пагинация появляется** только если записей > `paginate_by`.
- **Стандарт:** 9 элементов на страницу.
- **Исключение:** Уведомления (`paginate_by = 20`).

#### 4.5.3. Критические ошибки, которых нужно избегать:
```python
# ❌ НЕПРАВИЛЬНО - НЕ ДЕЛАТЬ:
context['page_obj'] = context['object_list']  # Ломает пагинацию!

# ✅ ПРАВИЛЬНО - Django автоматически создает page_obj:
# Ничего не делать, Django сам создает page_obj при paginate_by
```

#### 4.5.4. AJAX пагинация и фильтрация (КРИТИЧЕСКИ ВАЖНО для стабильности):
- Единый обработчик через `unified_ajax_handler.js` или локальный JS с делегированием событий.
- Автоматическое обновление URL с `?page=N`.
- Сохранение состояния фильтров при переходе между страницами.

#### 4.5.5. Правила безопасной AJAX работы:
```javascript
// ❌ НЕПРАВИЛЬНО - сохранение ссылок на DOM элементы:
const paginationContainer = document.querySelector('.pagination');
// После AJAX paginationContainer становится "мертвой" ссылкой!

// ✅ ПРАВИЛЬНО - динамический поиск при каждом AJAX:
const currentPagination = document.querySelector('.pagination');
// Всегда актуальная ссылка на элемент

// ✅ ПРАВИЛЬНО - делегирование событий:
document.addEventListener('click', function(e) {
    const filterBtn = e.target.closest('.hero-filter-btn');
    if (!filterBtn) return;
    // Обработка...
});
```

### 4.X. Навигационная панель (navbar)

Начиная с версии 11.1 навигационная панель закреплена в верхней части экрана на **всех** страницах.

- Используется класс `fixed-top` Bootstrap + кастомный CSS (`body { padding-top: 60px; }`).
- Высота панели фиксирована **60 px** (см. `static/css/project.css`).
- Логотип/бренд «Беседка» служит ссылкой на главную (`url 'home'`). Дополнительный пункт «Главная» в меню не обязателен.
- Все разделы автоматически учитывают этот отступ через глобальный padding для `<body>`.

> **Важно:** при изменении высоты панели необходимо синхронно обновить значение padding-top в `unified_styles.css` и переменную высоты в `project.css`.

---

## ⚡ 5. ИНТЕРАКТИВНОСТЬ И СТАТИСТИКА

### 5.1. AJAX и Анимации

- **AOS Анимации:** Плавное появление элементов при скролле с единой схемой задержек.
- **AJAX Фильтрация:** Единый JS-скрипт для всех `ListView` (`unified_ajax_handler.js`). Перехватывает клики, делает `fetch`, плавно обновляет контент в `#unified-cards-container`, обновляет URL (`history.pushState`) и вызывает `AOS.refresh()`.

### 5.2. Унифицированные счетчики (лайки, комментарии, просмотры)

Это **строгий стандарт** для ВСЕХ счетчиков во ВСЕХ карточках.

- **Поведение:** **НЕКЛИКАБЕЛЬНЫЕ ИНДИКАТОРЫ** (`pointer-events: none`).
- **Расположение:** Строго по центру карточки (`justify-content: center`).
- **Стили:** Стеклянный эффект, тени, увеличенные иконки (`1.1rem`) и текст (`0.95rem`).
- **ОБЯЗАТЕЛЬНАЯ ЦВЕТОВАЯ СХЕМА:**
  - **Лайки:** **Красный** (`#dc3545`)
  - **Комментарии:** **Синий** (`#0d6efd`)
  - **Просмотры / Гроурепорты:** **Зеленый** (`#198754`)

### 5.3. Необратимость действий (Система кармы)

- **Лайки и Комментарии:** Нельзя отменить или редактировать для обычных пользователей. Это обеспечивает целостность системы кармы и рейтингов.
- **Модерация:** Только персонал (`is_staff=True`) может управлять контентом.
- **Уведомления:** При попытке повторного действия выводится красивое центральное уведомление через `window.showNotification()`.

---

## 📋 6. ЧЕКЛИСТ ДЛЯ НОВЫХ РАЗДЕЛОВ

При создании нового раздела или страницы убедитесь, что:
- [x] View наследуется от `core.base_views.UnifiedListView` (или есть задокументированное отклонение).
- [x] Шаблон страницы наследуется от `templates/base_list_page.html` (или есть задокументированное отклонение).
- [x] Список карточек выводится через `_unified_cards_wrapper.html` и `unified_card_list`.
- [x] Hero-секция (если есть) использует партиал `unified_detail_hero.html` или соответствующие классы.
- [x] Все счетчики соответствуют стандарту (цвета, некликабельность, центрирование).
- [x] Все комментарии выводятся через `unified_comment_section.html`.
- [x] Все AJAX-запросы обрабатываются через единый `unified_ajax_handler.js` или по принципу делегирования событий.
- [x] К CSS и JS файлам добавлена версия для сброса кэша (`?v=YYYYMMDDHH`).
- [x] **Уведомления используют только `window.showNotification()` с правильными кнопками "Перейти".**
- [x] **Пагинация работает автоматически через `paginate_by` без перезаписи `page_obj`.**
- [x] **AJAX фильтрация использует делегирование событий и динамический поиск DOM элементов.**

---

## 📈 7. ИСТОРИЯ ИЗМЕНЕНИЙ

### v14.0 (15 июня 2025) - ГАРМОНИЗАЦИЯ ДОКУМЕНТАЦИИ SSOT
- **✅ УСТРАНЕНЫ:** Противоречия в документации SSOT.
- **✅ ДОБАВЛЕН:** Раздел `2. Управляемые отклонения от SSOT` с четкими правилами и примерами.
- **✅ СМЯГЧЕНЫ:** Абсолютные формулировки для отражения возможности управляемых отклонений.
- **✅ АКТУАЛИЗИРОВАН:** Чеклист для новых разделов с учетом правил отклонений.

### v13.3 (15 июня 2025) - ИСПРАВЛЕНИЕ AJAX ФИЛЬТРАЦИИ УВЕДОМЛЕНИЙ
- **🎯 РЕШЕНА:** Проблема "ошибки сети" при повторном нажатии кнопок фильтрации в уведомлениях
- **❌ УСТРАНЕНА:** Конфликт JavaScript обработчиков при AJAX обновлении DOM элементов
- **❌ УСТРАНЕНА:** Ошибка `initializeNotificationHandlers is not defined` в шаблоне уведомлений
- **✅ ВНЕДРЕНО:** Делегирование событий для стабильной работы AJAX фильтров
- **✅ ИСПРАВЛЕНО:** Динамический поиск элементов пагинации вместо сохранения "мертвых" ссылок
- **📝 ТЕХНИКА:** Использование `document.querySelector()` заново при каждом AJAX запросе

### v13.2 (15 июня 2025) - КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ ПАГИНАЦИИ
- **🏆 РЕШЕНА:** Корневая причина отсутствия пагинации во всех разделах
- **❌ УСТРАНЕНА:** Критическая ошибка в `core.base_views.py` - неправильная замена `page_obj` на `object_list`
- **❌ УСТРАНЕНА:** Дублирующая ошибка в `news/views.py` - перезапись `page_obj`
- **✅ ВОССТАНОВЛЕНА:** Автоматическая работа Django ListView пагинации
- **✅ ПРОВЕРЕНО:** Все разделы теперь показывают пагинацию согласно правилу >9 записей
- **📖 ЗАДОКУМЕНТИРОВАНО:** Полные стандарты SSOT пагинации в разделе 3.5

### v13.1 (15 июня 2025) - Исправления системы уведомлений
- **✅ ИСПРАВЛЕНЫ:** Критические проблемы синхронизации счетчиков навигации и hero-секции
- **✅ ДОБАВЛЕНЫ:** Недостающие SSOT стили для `hero-filter-btn` с glassmorphism эффектами
- **✅ ВОССТАНОВЛЕНА:** Стандартная Django пагинация вместо кнопки "Показать еще"
- **✅ ИСПРАВЛЕНА:** Структура данных в `NotificationListView` для правильной передачи контекста

### v13.0 (15 июня 2025) - Завершено тестирование системы уведомлений
- **✅ ПРОТЕСТИРОВАНЫ:** Все типы уведомлений (глобальные, персональные, системные)
- **✅ ПРОВЕРЕНЫ:** Кнопки "Перейти" корректно работают во всех уведомлениях
- **✅ ВАЛИДИРОВАНЫ:** Красивые центральные уведомления с правильной анимацией
- **✅ ЗАДОКУМЕНТИРОВАНЫ:** Полные стандарты UI/UX для системы уведомлений
- **✅ ИНТЕГРИРОВАНЫ:** Уведомления с SSOT-архитектурой

### v12.0-12.1 (13-14 июня 2025) - Завершение SSOT-реформы
- **ЧТО СДЕЛАНО:** Документ полностью переписан для устранения противоречий и устаревшей информации. Создан единый, актуальный стандарт UI/UX. Полная унификация магазина.
- **ССЫЛКИ:** [BESEDKA_MASTER_DOCUMENTATION.md](BESEDKA_MASTER_DOCUMENTATION.md)

### v10.0 - v11.0 (9-12 июня 2025) - Итеративная унификация
- **ЧТО СДЕЛАНО:** Были унифицированы счетчики, карточки, система лайков, навигационные кнопки. Введено понятие необратимости действий. Увеличены карточки галереи, исправлены "плавающие" элементы.
- **РЕЗУЛЬТАТ:** Эти изменения легли в основу текущего стандарта v12.0.

### v9.0 (Начало июня 2025) - Первые шаги к унификации
- **ЧТО СДЕЛАНО:** Попытки унифицировать счетчики и лайки, введение цветовых схем.
- **ПРОБЛЕМЫ:** Неполное внедрение, конфликты стилей, что потребовало полной переработки в v10.0.
