# 🎨 Стандарты UI/UX проекта "Беседка"

**Версия:** 8.1 - Логика пустого состояния при фильтрации (июнь 2025)
**Дата обновления:** 9 июня 2025 года
**Статус:** **ЭТАЛОН** для всех компонентов интерфейса

---

## 📚 СВЯЗАННЫЕ ДОКУМЕНТЫ

- **[BESEDKA_MASTER_DOCUMENTATION.md](BESEDKA_MASTER_DOCUMENTATION.md)** — Главный документ проекта.
- **[BESEDKA_USER_SYSTEM.md](BESEDKA_USER_SYSTEM.md)** — Эталонная система ролей и пользователей.
- **[MASTER_COMPLETION_PROMPT.md](MASTER_COMPLETION_PROMPT.md)** — Детальный план завершения проекта.

---

## 🏛️ 1. ОБЩИЕ ПРИНЦИПЫ И АРХИТЕКТУРА

Весь интерфейс проекта "Беседка" строится на принципах **консистентности, современности и производительности**. Раздел **"Галерея"** является архитектурным эталоном, на который должны ориентироваться все остальные разделы.

### 1.1. Эталонная архитектура шаблона

Каждая страница-список (галерея, гроу-репорты и т.д.) должна следовать унифицированной структуре, разделяя основной "скелет" страницы и динамически обновляемые части (партиалы).

- **Основной шаблон (`gallery_modern.html`, `growlogs/list.html` и т.д.):** Содержит `{% extends "base.html" %}`, структуру `hero-секции`, контейнеры для фильтров и контента.
- **Частичные шаблоны (`partials/`):** Содержат только циклы для рендеринга карточек и пагинацию. Используются как для первичной загрузки, так и для всех AJAX-запросов.

### 1.2. Унифицированная файловая структура
```
[app_name]/
├── templates/
│   ├── [app_name]/
│   │   ├── [list_page].html           # УНИФИЦИРОВАННЫЙ ОСНОВНОЙ ШАБЛОН
│   │   └── partials/
│   │       ├── _cards.html            # Карточки для AJAX
│   │       └── _pagination.html       # Пагинация для AJAX
├── views.py                           # ListView с поддержкой AJAX
static/
└── css/
    └── [app_name].css                 # Специфичные стили раздела
```
- **`static/css/unified_hero_buttons.css`** - ОБЯЗАТЕЛЕН к подключению на всех страницах с hero-секцией.

---

## 🎨 2. ЦВЕТОВЫЕ СХЕМЫ И ПЕРЕМЕННЫЕ

Цветовая схема определяется автоматически в зависимости от класса родительского контейнера `[section]-hero`.

```css
/* ГАЛЕРЕЯ - ФИОЛЕТОВЫЙ ГРАДИЕНТ */
.gallery-hero { --section-primary: #6f42c1; --section-secondary: #563d7c; }

/* ГРОУРЕПОРТЫ - ЗЕЛЕНЫЙ ГРАДИЕНТ */
.growlogs-hero { --section-primary: #28a745; --section-secondary: #20c997; }

/* НОВОСТИ - СИНИЙ ГРАДИЕНТ */
.news-hero { --section-primary: #007bff; --section-secondary: #0056b3; }

/* МАГАЗИН - ЗЕЛЕНЫЙ ГРАДИЕНТ */
.store-hero { --section-primary: #28a745; --section-secondary: #1e7e34; }
```

---

## 🔩 3. УНИФИЦИРОВАННЫЕ КОМПОНЕНТЫ

### 3.1. Hero-секция (Плашка)

Компактная, информативная секция вверху страницы.

- **Стили:**
  - `padding: 1.25rem 0`
  - `margin-bottom: 1.5rem`
  - `border-radius: 1rem`
  - `background: linear-gradient(135deg, var(--section-primary), var(--section-secondary))`
  - `box-shadow: 0 4px 20px rgba(..., 0.3)`

- **Структура:**
  - `h1.hero-title` - Заголовок
  - `p.hero-subtitle` - Подзаголовок
  - `.hero-stats-and-actions` - контейнер для статистики и кнопок
    - `.hero-stats` - контейнер для счетчиков (`.stat-item`)
    - `.hero-actions` - контейнер для кнопок (`.hero-btn`, `.hero-btn-secondary`)

### 3.2. Hero-кнопки (Эталон v23.0)

"Живые" 3D-кнопки с постоянной анимацией.

- **Общие свойства:**
  - **3D-эффект:** Глубокая многослойная тень по умолчанию, которая усиливается при наведении, а сама кнопка "всплывает" на `6px`.
  - **Размеры:** `padding: 16px 32px`, `font-size: 1.15rem`.
  - **Скругление:** `border-radius: 14px`.

- **Основная кнопка `.hero-btn`:**
  - **Вид:** Цветной градиент (зависит от раздела).
  - **Анимация:** Постоянное, бесшовное переливание градиента (`shimmer`) в течение 5 секунд.

- **Вторичная кнопка `.hero-btn-secondary`:**
  - **Вид:** Полупрозрачная "стеклянная" (`backdrop-filter: blur(10px)`).
  - **Анимация:** Постоянный, бесшовный "скользящий блик" (`glare`) в течение 5 секунд.

### 3.3. Адаптивные счетчики

Используются внутри Hero-секции.

- **Стили:**
  - `background: rgba(255, 255, 255, 0.15)`
  - `backdrop-filter: blur(10px)`
  - `border-radius: 12px`
  - `padding: 1rem`
  - `max-width: 120px` (адаптивная ширина)
- **Содержимое:**
  - `.stat-number` (шрифт `1.3rem`)
  - `.stat-label` (шрифт `0.7rem`, `text-transform: uppercase`)

### 3.4. Фильтры (Glassmorphism)

- **Стили контейнера `.filters`:**
  - `background: rgba(255, 255, 255, 0.95)`
  - `backdrop-filter: blur(20px)`
  - `border-radius: 1rem`
  - `padding: 0.75rem`
- **Стили кнопок `.filter-tab`:**
  - `padding: 0.5rem 1rem`, `border-radius: 1.5rem`
  - `.active` состояние имеет градиент `var(--section-primary) -> var(--section-secondary)`

### 3.5. Карточки контента (Masonry Layout)

- **Принцип "Вся карточка - ссылка":** Вся область карточки является кликабельной ссылкой, ведущей на детальную страницу.
- **Интерактивные элементы:** Лайки, комментарии и прочие действия доступны **только** на детальной странице. На самой карточке они отображаются как некликабельные индикаторы.
- **Стили:**
  - `flex: 0 1 calc(33.333% - 14px)` (3 колонки на десктопе, 2 на планшете, 1 на мобильном).
  - `background: white`, `border-radius: 15px`, `min-height: 350px`.
  - **Hover-эффект:** `transform: translateY(-4px)` (всплытие карточки) и `transform: scale(1.05)` (увеличение изображения).
  - Изображение: `height: 200px`, `object-fit: cover`.

---

## ⚡ 4. ИНТЕРАКТИВНОСТЬ

### 4.1. AOS Анимации

Используется для плавного появления элементов при скролле.
- **Инициализация:** `AOS.init({ duration: 600, easing: 'ease-out', once: true, offset: 50 });`
- **Стандартная схема задержек (`data-aos-delay`):**
  - Hero-секция: `0`
  - Заголовок: `100`
  - Подзаголовок: `200`
  - Статистика и кнопки: `300`
  - Счетчики и кнопки (детально): `400` - `850`
  - Карточки: без задержки, `data-aos="fade-up"`

### 4.2. AJAX Фильтрация и Пагинация

- **Принцип:** Все `ListView` должны поддерживать AJAX-запросы, возвращая только частичный шаблон (`_cards.html`), содержащий список элементов и пагинацию.
- **Frontend:** Единый JS-скрипт перехватывает клики по фильтрам/пагинации, делает `fetch`-запрос, плавно обновляет контент в div-контейнере, обновляет URL через `history.pushState` и вызывает `AOS.refresh()`.
- **Умная прокрутка:** Скрипт плавно прокручивает страницу к началу списка, только если пользователь прокрутил достаточно далеко вниз.

### 4.3. Необратимость действий (Система кармы)

- **Лайки и Комментарии:** Являются **необратимыми** для обычных пользователей. Они не могут отменить лайк или отредактировать/удалить свой комментарий.
- **Модерация:** Только персонал (`is_staff=True`) имеет право редактировать и удалять любой контент. Кнопки управления контентом скрыты от обычных пользователей в шаблонах.

### 4.4. Пустое состояние при фильтрации

- **Умные сообщения:** При фильтрации показываются специфические сообщения в зависимости от типа фильтра:
  - `growlog`: "Нет фотографий из гроу-репортов"
  - `popular`: "Нет популярных фотографий"
  - `commented`: "Нет обсуждаемых фотографий"
- **Кнопка загрузки:** Показывается ТОЛЬКО если:
  - Это страница "Мои фото" (любой фильтр)
  - Или основная галерея без активной фильтрации (фильтр `newest`)
- **Логика:** Кнопки фильтрации должны только фильтровать содержимое, а не предлагать добавление контента. Это обеспечивает интуитивно понятный пользовательский опыт.

---

## 📋 5. ЧЕКЛИСТ ДЛЯ НОВЫХ РАЗДЕЛОВ

При создании нового раздела или страницы убедитесь, что:
- [ ] Подключен `unified_hero_buttons.css`.
- [ ] Используется компонент Hero-секции с правильными классами (`[section]-hero`) для автоматического окрашивания.
- [ ] Используются классы `.hero-btn` и `.hero-btn-secondary` для кнопок.
- [ ] Применены AOS-анимации со стандартными задержками.
- [ ] Если есть списки, реализована AJAX-фильтрация по эталону Галереи.
- [ ] Карточки контента следуют принципу "вся карточка - ссылка".
- [ ] Интерактивные элементы (лайки/комментарии) вынесены на детальную страницу.
- [ ] Проверена адаптивность на всех разрешениях.
- [ ] К CSS-файлам добавлена версия для сброса кэша (`?v=YYYYMMDDHH`).

---
