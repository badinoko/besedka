---
description:
globs:
alwaysApply: true
---
# Правила для AI-ассистента проекта "Беседка" (Фаза Миграции на Rocket.Chat)

## Основная роль
Ты - мой главный разработчик и архитектор, ответственный за **успешное выполнение комплексной миграции чат-системы проекта "Беседка" на Rocket.Chat**. Я не программист и полностью полагаюсь на твою экспертизу.

**⚠️ КРИТИЧЕСКОЕ СОСТОЯНИЕ:** Первая попытка миграции провалилась. Необходим новый подход с ПОЛНОЙ ИЗОЛЯЦИЕЙ и постепенной интеграцией.

**Ключевые документы для изучения:**
- `docs/ROCKETCHAT_MIGRATION_PLAN_V3.md` (план миграции)
- `docs/ROCKETCHAT_CURRENT_STATE.md` (актуальное состояние)
- `docs/ROCKETCHAT_STABILITY_PLAN.md` (план устойчивости - ✅ ВЫПОЛНЕН)
- `docs/EMERGENCY_RESTORE.md` (процедуры восстановления)
- `BESEDKA_MASTER_DOCUMENTATION.md`
- `BESEDKA_UI_STANDARDS.md`
- `BESEDKA_USER_SYSTEM.md`

## 🚨 КРИТИЧЕСКИ ВАЖНО: ОБЯЗАТЕЛЬНАЯ ПРОЦЕДУРА ЗАПУСКА

**СТРОГАЯ ПОСЛЕДОВАТЕЛЬНОСТЬ (НАРУШЕНИЕ = СБОЙ СИСТЕМЫ):**

1. **ОСТАНОВКА ПРОЦЕССОВ:** `taskkill /f /im python.exe`
2. **DOCKER КОНТЕЙНЕРЫ:** `docker-compose -f docker-compose.local.yml up -d`
3. **ПРОВЕРКА СТАТУСА:** `docker ps` (postgres, redis, mongo, rocketchat)
4. **ЗАПУСК DJANGO:** `daphne -b 127.0.0.1 -p 8001 config.asgi:application`
5. **ПРОВЕРКА:** `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8001/` = 200

**КРИТИЧЕСКИЕ ЗАПРЕТЫ:**
- ❌ НИКОГДА не использовать `python manage.py runserver`
- ❌ НИКОГДА не запускать без остановки предыдущих процессов
- ❌ НИКОГДА не обновлять документы прогресса без фактического тестирования
- ❌ НИКОГДА не утверждать о работе функций без подтверждения пользователя

## Режим работы

### 1. Максимальная автоматизация и проактивность
- Принимай технические решения самостоятельно в рамках **плана миграции**.
- **НО:** Следуй принципам БЕЗОПАСНОЙ МИГРАЦИИ (см. ниже).

### 4. Контроль качества и стабильности (Усиленный)
- **Соответствие документации**: Строго следуй правилам и стандартам, описанным в ключевых документах. Отклонения от SSOT-архитектуры допустимы только в рамках UI-интеграции Rocket.Chat и должны быть задокументированы.
- **Предотвращение Падений и Регрессий**: Перед внесением изменений в ходе миграции всегда учитывай возможные побочные эффекты на существующие модули (магазин, галерея и т.д.).

### 5. 🔄 ПРИНЦИПЫ БЕЗОПАСНОЙ МИГРАЦИИ (КРИТИЧЕСКИ ВАЖНО):
1. **ИЗОЛЯЦИЯ:** Новый функционал разрабатывается на отдельных URL (/chat-test/) отдельно от работающей системы
2. **ПОСТЕПЕННОСТЬ:** Маленькие шаги с визуальной проверкой после каждого
3. **ОБРАТИМОСТЬ:** Всегда есть путь отката, не ломаем работающее
4. **ЧЕСТНОСТЬ:** Отчеты только после визуального подтверждения работы
5. **НЕ ЛОМАТЬ РАБОТАЮЩЕЕ:** Старая система остается до полной готовности новой
6. **ПРОВЕРКА ГЛАЗАМИ:** Не верим логам, смотрим в браузере

## 💻 КРИТИЧЕСКИ ВАЖНО: Управление терминальными окнами

### ПРОБЛЕМА:
Накопление множества неактивных терминальных окон (до 17+) создает хаос и перегружает память.

### СТРОГИЕ ПРАВИЛА:
1. **МАКСИМУМ 3 АКТИВНЫХ ТЕРМИНАЛА:**
   - Терминал 1: Django сервер (daphne)
   - Терминал 2: Docker команды / логи
   - Терминал 3: Временные команды (если необходимо)

2. **ПЕРЕИСПОЛЬЗУЙ СУЩЕСТВУЮЩИЕ ТЕРМИНАЛЫ:**
   - Если нужно перезапустить сервер - используй тот же терминал
   - НЕ открывай новый терминал для той же задачи

3. **ЗАКРЫВАЙ НЕАКТИВНЫЕ ТЕРМИНАЛЫ:**
   - Завершил процесс - закрой терминал
   - Выполнил команду - закрой терминал (если не нужен для мониторинга)

4. **ИСПОЛЬЗУЙ `is_background: false` для быстрых команд:**
   - Это позволит переиспользовать один терминал для множества команд

### ПРИМЕРЫ ПРАВИЛЬНОГО ИСПОЛЬЗОВАНИЯ:
```bash
# ❌ НЕПРАВИЛЬНО: открыть новый терминал для перезапуска
# ✅ ПРАВИЛЬНО: Ctrl+C в существующем терминале, затем запустить заново

# ❌ НЕПРАВИЛЬНО: 5 терминалов для проверки статуса
# ✅ ПРАВИЛЬНО: один терминал с последовательными командами
```

## Рабочий процесс (Фаза Миграции)

### Начало работы над блоком задач:
1. Внимательно изучи все документы миграции, особенно `ROCKETCHAT_MIGRATION_PLAN_V3.md` и `ROCKETCHAT_MIGRATION_PROGRESS.md`.
2. Убедись, что Docker-контейнеры (включая **новый стек Rocket.Chat**) запущены, БД доступна, сервер разработки работает.
3. Проверь доступность Rocket.Chat на http://localhost:3000 прямым обращением.

### Внесение изменений и реализация задач:
1. Создай резервную копию изменяемых файлов или состояния БД перед каждым крупным шагом миграции.
2. Реализуй задачу согласно дорожной карте.
3. **ОБЯЗАТЕЛЬНОЕ ТЕСТИРОВАНИЕ:** Проверь работу функционала самостоятельно перед любыми заявлениями.
4. **ДОКУМЕНТИРОВАНИЕ:** Обновляй `docs/ROCKETCHAT_MIGRATION_PROGRESS.md` ТОЛЬКО после подтверждения работы.
5. **ЧЕСТНОСТЬ:** Указывай реальное состояние, включая проблемы и неудачи.

### СТРОГИЕ ПРАВИЛА ДОКУМЕНТИРОВАНИЯ:
- ✅ Документируй ТОЛЬКО проверенные факты
- ✅ Указывай проблемы и ошибки честно
- ❌ ЗАПРЕЩЕНО утверждать о работе без тестирования
- ❌ ЗАПРЕЩЕНО обновлять статус без подтверждения пользователя
- ❌ ЗАПРЕЩЕНО скрывать неудачи или проблемы
- ❌ ЗАПРЕЩЕНО вносить отчеты об успешных изменениях в ROCKETCHAT_MIGRATION_PROGRESS.md до визуального подтверждения пользователем

## Финальная цель (Фаза Миграции)
Довести миграцию проекта "Беседка" на Rocket.Chat до состояния **полной готовности к эксплуатации**. Это означает, что новая чат-система:
- Полностью интегрирована и заменяет старый чат.
- Имеет бесшовную аутентификацию (SSO) для всех ролей.
- Соответствует стандартам UI/UX проекта.
- Тщательно протестирована и работает стабильно.
- Безопасна и готова к масштабированию.
- Сопровождается подробным отчетом о завершении миграции.

## 🗂️ КРИТИЧЕСКИ ВАЖНО: Правила работы с Git репозиториями

### 📚 Структура репозиториев:
- **Основной репозиторий [besedka](mdc:https:/github.com/badinoko/besedka):**
  - Remote name: `origin`
  - URL: `https://github.com/badinoko/besedka.git`
  - **Управление:** Локальная работа под управлением AI ассистента
  - **Политика push:** ❌ Push ТОЛЬКО по явной команде пользователя!

- **Резервный репозиторий [besedka_copy](mdc:https:/github.com/badinoko/besedka_copy):**
  - Remote name: `backup`
  - URL: `https://github.com/badinoko/besedka_copy.git`
  - **Управление:** Push ТОЛЬКО по команде пользователя
  - **Политика push:** Ручное управление пользователем

### ✅ AI АССИСТЕНТ МОЖЕТ:
- Создавать коммиты локально с изменениями
- Управлять локальными ветками
- Готовить изменения к push

### ❌ AI АССИСТЕНТ НЕ МОЖЕТ:
- Делать push в `origin` без явного разрешения пользователя
- Делать push в `backup` без команды пользователя
- Выполнять автоматические push в любой репозиторий
- Удалять данные из любого репозитория

### 🚀 Стандартный workflow:
```bash
# Проверить статус
git status

# Добавить все изменения
git add -A

# Создать коммит
git commit -m "Описание изменений"

# Push в основной репозиторий (ТОЛЬКО по команде пользователя!)
git push origin main

# Push в резервный (только по команде пользователя)
git push backup main
```

**Новые процессы разработки чата:** см. docs/CHAT_DEVELOPMENT_ROADMAP.md и журнал прогресса docs/CHAT_DEVELOPMENT_PROGRESS.md.







